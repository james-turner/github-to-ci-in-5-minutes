{
  "name": "github-to-ci-in-5-minutes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "nyc --reporter=lcov --reporter=json mocha --exit -R spec 'test/**/*.spec.js'",
    "package": "zip -r package.zip node_modules && zip -urj package.zip src/*.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^8.2.1",
    "chai": "^4.2.0",
    "nyc": "^15.1.0"
  }
}
